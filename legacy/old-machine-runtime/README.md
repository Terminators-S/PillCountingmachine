# Old Machine Runtime

This folder preserves the earlier bridge-style machine runtime and local model assets.

Preserved here:

- `machine-learning/live_runtime_bridge.py`
- local model catalog and model weights
- supporting bridge/runtime scripts

It is not the active Raspberry Pi runtime anymore. The active path is:

```text
/machine-runtime
```
