# Old Platform Experiments

This folder preserves older UI and server experiments that are no longer the active product path.

Included:

- `web-spa-prototype/`
- `static-prototype/`

These files are kept so earlier work is not lost, but new development should stay in:

- `apps/web/`
- `apps/api/`
- `machine-runtime/`
