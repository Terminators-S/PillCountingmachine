# Machine MVP environment defaults.
# This file is optional for the first milestone.
# Keep the first milestone local-first and offline-friendly.

MACHINE_NAME=pill-counter-pi
RUN_OUTPUT_DIR=runs
API_SYNC_ENABLED=false
API_SYNC_URL=
ENABLE_PREVIEW=true
